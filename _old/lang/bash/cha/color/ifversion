## CSS Hex Value
if [[ $# == 1 ]]; then
  if [[ ${#1} == 6 ]]; then
    echo six
  elif [[ ${#1} == 3 ]]; then
    echo 3
  elif [[ ${#1} == 1 ]]; then
    echo 1
  else
    echo "${red}Must be 1, 3, or 6 in length.$reset"
    exit 1
  fi
  exit 0
fi
